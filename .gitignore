out

# SDK zips pulled from a CI run for tools/build-local.ps1
.local-deps/

# Per-worktree app state (URNETWORK_APP_ROOT), so concurrent worktrees do not
# share one LocalState/rpc_session/log. Never committed.
.localstate*/
