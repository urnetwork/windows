<?xml version="1.0" encoding="utf-8"?>
<!-- URnetwork Common static library: protocol, pipe transport, paths, logging,
     SDK bootstrap. Shared by the service and the tray app.
     SPDX-License-Identifier: MPL-2.0 -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Debug|x64"><Configuration>Debug</Configuration><Platform>x64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Release|x64"><Configuration>Release</Configuration><Platform>x64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Debug|ARM64"><Configuration>Debug</Configuration><Platform>ARM64</Platform></ProjectConfiguration>
    <ProjectConfiguration Include="Release|ARM64"><Configuration>Release</Configuration><Platform>ARM64</Platform></ProjectConfiguration>
  </ItemGroup>

  <PropertyGroup Label="Globals">
    <VCProjectVersion>17.0</VCProjectVersion>
    <ProjectGuid>{A1B2C3D4-0001-4E5F-8A9B-000000000001}</ProjectGuid>
    <RootNamespace>UrnetworkCommon</RootNamespace>
    <ConfigurationType>StaticLibrary</ConfigurationType>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />

  <PropertyGroup Label="Configuration">
    <UseDebugLibraries Condition="'$(Configuration)'=='Debug'">true</UseDebugLibraries>
    <UseDebugLibraries Condition="'$(Configuration)'=='Release'">false</UseDebugLibraries>
    <WholeProgramOptimization Condition="'$(Configuration)'=='Release'">true</WholeProgramOptimization>
  </PropertyGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />

  <ItemDefinitionGroup>
    <ClCompile>
      <!-- DYNAMIC CRT (MD). Common.lib links into the WinUI App, which requires the
           dynamic CRT (the Windows App SDK + WinUI are built /MD); a static-CRT (/MT)
           Common.lib triggers LNK2038 RuntimeLibrary-mismatch + LNK4098. Service links
           Common too, so it is /MD as well (see Service.vcxproj) - the CRT flavor must
           match within, and across statically-linked, binaries. -->
      <RuntimeLibrary Condition="'$(Configuration)'=='Debug'">MultiThreadedDebugDLL</RuntimeLibrary>
      <RuntimeLibrary Condition="'$(Configuration)'=='Release'">MultiThreadedDLL</RuntimeLibrary>
      <Optimization Condition="'$(Configuration)'=='Release'">MaxSpeed</Optimization>
    </ClCompile>
  </ItemDefinitionGroup>

  <ItemGroup>
    <ClInclude Include="Ids.h" />
    <ClInclude Include="Log.h" />
    <ClInclude Include="Paths.h" />
    <ClInclude Include="PipeClient.h" />
    <ClInclude Include="PipeServer.h" />
    <ClInclude Include="Protocol.h" />
    <ClInclude Include="Sdk.h" />
    <ClInclude Include="Strings.h" />
  </ItemGroup>
  <ItemGroup>
    <ClCompile Include="Log.cpp" />
    <ClCompile Include="Paths.cpp" />
    <ClCompile Include="PipeClient.cpp" />
    <ClCompile Include="PipeServer.cpp" />
    <ClCompile Include="Sdk.cpp" />
    <ClCompile Include="Strings.cpp" />
  </ItemGroup>

  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
